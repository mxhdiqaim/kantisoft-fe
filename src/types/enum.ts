// eslint-disable-next-line
// @ts-expect-error
export enum OrderStatusEnum {
    CANCELED = "canceled",
    PENDING = "pending",
    COMPLETED = "completed",
}

// eslint-disable-next-line
// @ts-expect-error
export enum OrderPaymentMethodEnum {
    CARD = "card",
    CASH = "cash",
    TRANSFER = "transfer",
}
